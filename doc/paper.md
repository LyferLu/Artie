成都东软学院

本科毕业设计报告

题目：基于AI的文生图与图像智能编辑系统的设计与实现

| 学 院：    | 计算机与软件学院 |
|------------|------------------|
| 专 业：    | 软件工程         |
| 班 级：    | 软件工程24301    |
| 学生姓名： | 卢一博           |
| 学生学号： | 24010230127      |
| 指导教师： | 曹旭             |

2026年5月

# 摘 要

随着生成式人工智能（AIGC）技术的飞速发展，以扩散模型为代表的文生图技术已成为创意设计领域的重要生产力。然而，目前市面上的工具往往侧重于单一的图像生成，而忽略了后续精细化编辑的连贯性，导致用户在生成与修饰之间存在严重的工作流断层。针对这一痛点，本文设计并实现了一个集成化、本地化部署的基于AI的文生图与图像智能编辑系统。

在系统架构上，本系统采用前后端分离的模式。后端基于Python语言与FastAPI框架构建，通过集成Hugging Face开源生态中的Stable Diffusion系列模型，实现了高质量的文生图、AI扩图（Outpainting）、AI擦除、AI重绘及修复人脸与智能选区功能。同时，系统还整合了Real-ESRGAN模型用于解析度提升，以及REMBG模型实现一键去背景，构建了全方位的智能编辑矩阵。前端则基于React框架开发，重点攻克了高性能交互画布技术，支持图像的缩放、平移以及像素级精确的蒙版绘制，解决了Web端复杂图像编辑的交互难题。

在实现过程中，本文重点探讨并解决了<补充内容>。经测试验证，该系统运行稳定，功能涵盖了从文本创意到高质量图像产出并进行二次智能编辑的全流程，显著提升了创作者的处理效率，为一站式AIGC应用工具的开发提供了实践范例。

**关键词**：人工智能生成内容；扩散模型；图像编辑；FastAPI；图像修复；本地部署

Abstract

With the rapid development of Artificial Intelligence Generated Content (AIGC), text-to-image technology represented by diffusion models has become a significant productivity force in the field of creative design. However, current tools often focus on single-image generation while neglecting the coherence of subsequent refined editing, leading to a severe workflow disconnect between generation and modification for users. Addressing this pain point, this paper designs and implements an integrated, locally deployed AI-based text-to-image and intelligent image editing system.

In terms of system architecture, the system adopts a decoupled frontend-backend model. The backend is built on Python and the FastAPI framework. By integrating Stable Diffusion series models from the Hugging Face open-source ecosystem, it achieves high-quality text-to-image, AI outpainting, and AI inpainting (erasure and restoration) functions. Meanwhile, the system integrates the Real-ESRGAN model for resolution enhancement and the rembg model for one-click background removal, constructing a comprehensive intelligent editing matrix. The frontend is developed based on the React framework, focusing on high-performance interactive canvas technology that supports image scaling, panning, and pixel-accurate mask drawing, which solves the interactive challenges of complex image editing on the Web.

<翻译>

**Key Words**: AIGC; Diffusion Models; Image Editing; FastAPI; Inpainting; Local Deployment

目 录

第1章 绪论

1.1 选题背景

1.2 选题目的

1.3 国内外现状

1.4 主要研究内容

1.4.1 系统概述

1.4.2 功能概述

1.4.3 性能优化与交互逻辑研究

1.5 报告内容组织结构

第2章 关键技术介绍

2.1 关键性开发技术介绍

2.1.1 扩散模型与 Stable Diffusion

2.1.2 FastAPI 异步 Web 框架

2.1.3 基于 React 与 TypeScript 的前端体系

2.2 其它相关技术

2.2.1 Hugging Face diffusers 库

2.2.2 PyTorch 深度学习框架

2.2.3 CUDA 硬件加速

2.2.4 辅助增强插件模型

第3章 系统分析

3.1 系统构架概述

3.2 需求分析

3.2.1 用户需求分析

3.2.2 业务需求分析

3.2.3 功能需求分析

3.2.4 业务建模

3.3 用例规约

3.3.1 创作生成模块用例

3.3.2 编辑类功能模块用例

3.3.3 非编辑类功能模块用例

3.4 系统可行性分析

3.4.1 技术可行性 

3.4.2 经济可行性 

3.4.3 操作可行性

3.4.4 系统安全性分析

3.5 系统开发环境

第4章 系统设计

4.1 系统架构设计

4.2 系统功能模块整体设计

4.3 系统功能模块详细设计

4.3.1 文生图创作模块设计

4.3.2 AI擦除模块设计

4.3.3 AI扩图模块设计

4.3.4 AI重绘模块设计

4.3.5 去背景模块设计

4.3.6 超分辨率模块设计

4.3.7 修复人脸模块设计

4.3.8 智能选区模块设计

4.3.9 用户工作区模块设计

4.4 接口设计

4.4.1 创作型模块接口设计

4.4.2 编辑类模块接口设计

4.4.3 非编辑类模块接口设计

4.5 数据库设计

4.5.1 CDM图

4.5.2 数据库表结构设计

第5章 系统实现

5.1 接口实现

5.1.1 控制管理

5.1.2 接口服务器

5.2 接口设计说明

第6章 系统测试

6.1 测试方案及测试用例

6.1.1 发送数据

6.1.2 接收数据

6.2测试方法

第7章 结论

参考文献

# **第1章 绪论**

本设计针对当前生成式人工智能（AIGC）领域中图像生成与精细化编辑流程割裂的现状，旨在研发一套集成了文生图、AI 擦除、AI 扩图、AI重绘、去背景及超分辨率提升的一站式智能图像处理平台。本设计的目的在于降低高门槛 AI 技术的使用难度，通过本地化部署保障用户数据隐私。研究设想是以扩散模型（Diffusion Models）为核心算力，构建一个前后端分离的 Web 应用，采用原型开发法，通过集成 SOTA（业界顶尖）开源模型并优化显存调度，实现从文本创意到成品图像的闭环工作流。本选题依据在于 AIGC 技术的工程化应用正处于高速发展期，市场亟需从“单纯生成”向“生成与编辑一体化”转型的生产力工具。

## 1.1 选题背景

<填写选题背景>

## 1.2 选题目的

<填写选题目的>

## 1.3 国内外现状

在当前的图像生成技术研究领域中，扩散模型已成为生成式人工智能（AIGC）的核心底层架构。扩散模型通常被定义为一个双向的马尔可夫链过程，其核心逻辑在于通过前向扩散阶段逐步向原始图像数据中注入高斯噪声，并利用深度神经网络在逆向去噪阶段学习如何从纯噪声分布中精确还原出高质量的目标图像。这种基于概率分布演变的建模方式，有效克服了传统生成对抗网络（GAN）在训练稳定性与模式坍塌方面的局限，为实现高度复杂的语义映射与图像重构奠定了坚实的理论基础。

作为扩散模型领域最具代表性的工程实践，Stable Diffusion 通过引入潜在扩散模型（Latent Diffusion Model）架构，实现了图像生成效率与质量的平衡。该技术并不直接在像素空间进行高维计算，而是利用变分自编码器（VAE）将图像压缩至低维的潜在空间进行去噪处理，从而显著降低了对硬件推理算力的需求。这一突破使得高质量的图像编辑任务得以在个人计算设备上运行，在本项目的后端架构中，通过对 diffusers 库以及多种高性能采样器的深度集成，充分利用了这一技术特性来实现实时的局部重绘与图像外扩功能。

随着 Stable Diffusion 体系的日益成熟，全球 AIGC 产业正经历从宏观内容生成向微观精确控制的纵深演进。国际上，以 Stability AI 为首的开源力量与以 OpenAI、Adobe 为代表的商业巨头持续在模型底座、语义理解及专业流集成方向发力，研究重点已转向如何通过 ControlNet、BrushNet 等技术实现对图像布局与细节的像素级操控。国内方面，百度、阿里巴巴及腾讯等头部科技公司依托大规模预训练算力，相继推出了具备本土化语义理解优势的大模型，同时国内开源社区在针对特定风格的轻量化微调模型以及针对中文字符生成的 AnyText 算法落地应用上也表现出极高的活跃度。

尽管国内外技术储备已非常雄厚，但在实际应用层面仍面临着显著的门槛。目前主流的专业级 WebUI 工具界面极其复杂且参数冗冗，导致非专业人员的学习成本过高；而现有的商业化云端解决方案则普遍存在用户隐私泄露风险及高昂的订阅费用。因此，针对个人用户和专业创作者的需求，开发一个集成 LaMa 语义填充、交互式目标分割以及高精度图像增强等多种前沿算子的轻量化集成系统，并支持完全的本地化部署，不仅符合当前技术向终端下沉的趋势，也具备极高的应用价值与现实意义。

## 1.4 主要研究内容

### 1.4.1 系统概述

<填写系统概述>

### 1.4.2 功能概述

<填写功能概述，主要围绕文生图、AI扩图、AI擦除、AI重绘、去背景、超分辨率、人脸修复、智能选区>

### 1.4.3 性能优化与交互逻辑研究

针对本地化部署的性能瓶颈，本课题重点研究了 AI 推理过程中的资源调度优化方案。后端源码通过显式控制 torch.device 与半精度浮点数（FP16）运算，大幅降低了扩散模型在推理阶段的瞬时显存峰值。同时，前端交互层面通过研发高级 HTML5 Canvas 绘图组件，实现了像素级的掩膜编辑与实时预览效果，极大地提升了用户对 AI 生成过程的控制精度。研究还涵盖了对多种跨平台环境的适配支持，通过自动化脚本与容器化技术简化了底层复杂深度学习环境的配置流程，真正实现了高门槛 AI 技术的轻量化集成与平民化应用。

## 1.5 报告内容组织结构

本文共分为七个章节，各章节内容安排如下：

第1章为绪论，主要介绍课题的研究背景、研究意义以及国内外研究现状，并对本文的研究内容进行概述；

第2章为相关技术介绍，主要阐述系统开发过程中涉及的关键技术；

第3章为系统分析，对系统的功能需求及可行性进行分析；

第4章为系统设计，介绍系统总体架构及各模块设计；

第5章为系统实现，详细说明各功能模块的实现过程；

第6章为系统测试，对系统功能进行测试与结果分析；

第7章为总结与展望，对全文进行总结并提出后续改进方向。

# **第2章 关键技术介绍**

## 2.1 关键性开发技术介绍

### 2.1.1 扩散模型与 Stable Diffusion

本项目的图像生成逻辑高度依赖于 Stable Diffusion 系列模型及其在 Hugging Face diffusers 库中的实现。diffusers 是目前业界领先的扩散模型集成方案，提供了从调度器（Schedulers）到管道（Pipelines）的完整封装。系统通过集成该技术，实现了 Stable Diffusion XL 1.0 等前沿模型的调用，能够高质量地完成局部重绘（Inpainting）与图像外扩（Outpainting）任务。通过利用 safetensors 格式存储模型权重，系统进一步增强了模型加载的安全性与速度。

### 2.1.2 FastAPI 异步 Web 服务框架

为了保障图像处理请求的高并发处理能力与响应速度，系统后端采用了 FastAPI 作为 Web 服务框架。FastAPI 是一种基于 Python 3.7+ 标准类型提示的现代、高性能 Web 框架，其核心优势在于原生支持异步编程（async/await），极大提升了 I/O 密集型任务的处理效率。本项目通过 FastAPI 定义了标准化的 RESTful API 接口，负责模型加载状态查询、图像上传下载以及推理任务的调度管理。此外，结合 Pydantic 库进行的数据模型校验，确保了前后端数据交互的严格一致性与系统健壮性。

### 2.1.3 基于 React 与 TypeScript 的前端体系

系统前端采用 React 框架构建，并全面使用 TypeScript 编程语言以增强代码的可维护性与类型安全性。React 的组件化开发模式使得复杂的用户交互界面，如实时绘图画布、模型参数调节面板等，能够被高效地解耦与复用。TypeScript 的引入则通过静态类型检查，在开发阶段即能发现潜在的逻辑错误，尤其在处理复杂的图像元数据和异步 API 响应时提供了坚实的基础。在构建工具方面，项目选用了 Vite 进行项目的快速打包与热更新，显著提升了前端开发与线上部署的效率。

## 2.2 其它相关技术

### 2.2.1 Hugging Face diffusers 库

diffusers 是 Hugging Face 提供的扩散模型标准库，是本系统实现模型调度的底层支柱。它将复杂的扩散逻辑封装为标准化的 Pipeline（流水线），支持 Stable Diffusion 1.5、SDXL 等多种开源大模型，并提供了调度器（Schedulers）优化技术，能在保证画质的前提下显著缩短生成时间。

### 2.2.2 PyTorch 深度学习框架

本系统后端核心算法的运行依赖于 PyTorch 深度学习框架。作为目前主流的开源张量计算与神经网络库，PyTorch 提供了强大的自动求导系统以及对算子级别的灵活控制能力，使其能够支撑 LaMa 以及各种扩散模型（Diffusion Models）的高效推理。在本项目的环境配置中，系统要求 Torch 版本需达到 2.0.0 以上，以利用其在模型量化、编译优化及分布式训练方面的最新特性。同时，结合项目中的 accelerate 与 peft 库，系统能够实现对显存占用的精细化管理，并支持在消费级显卡甚至是仅有 CPU 的环境下进行平滑运行。

### 2.2.3 CUDA 硬件加速

由于本系统采用本地部署方案，图像生成极其依赖计算资源。系统通过 NVIDIA CUDA 架构实现深度学习任务的硬件加速。利用 PyTorch 框架直接调用显卡内核，将原本在 CPU 上需耗时数分钟的计算任务缩短至秒级，是实现实时 AI 编辑的前提。

### 2.2.4 辅助增强插件模型

为了扩展图像编辑的功能边界，系统还集成了一系列基于计算机视觉的辅助插件。例如，利用 Segment Anything (SAM) 插件实现快速、精准的目标物体分割；通过 RealESRGAN 技术实现生成图像的超分辨率增强；以及采用 GFPGAN 或 RestoreFormer 插件进行人脸特征的修复与增强。这些辅助技术的加入，使得本系统不再局限于单一的重绘功能，而是进化为一个综合性的 AI 图像编辑平台。

# **第3章 系统分析**

## 3.1 系统构架概述

本系统采用了 B/S（Browser/Server）架构，旨在为用户提供一站式的 AI 图像生成与智能编辑服务。系统前端采用 React 框架构建，利用其高效的组件化开发能力，为用户提供响应迅速、交互直观的创作界面，特别是在处理复杂的 Canvas 画布交互时表现优异。

系统后端基于异步高性能框架 FastAPI 构建，作为整个系统的逻辑调度中心。后端通过调用部署在本地的 PyTorch 环境及 Hugging Face diffusers 库，实现对 Stable Diffusion、Real-ESRGAN、U2-Net 等多个深度学习模型的托管与推理。考虑到 AI 推理任务属于典型的计算密集型且高耗时操作，后端设计了异步任务队列机制，避免了 HTTP 请求阻塞。

在数据持久化方面，系统采用 MySQL 存储用户信息、生成历史元数据及任务日志。生成的图像文件则存储在服务器本地的文件系统中，数据库仅保留其路径索引。这种架构设计既保证了系统能够处理复杂的 AI 算力需求，又确保了前端交互的轻量化与便捷性。

## 3.2 需求分析

### 3.2.1 用户需求分析

用户希望通过该平台，仅需输入简单的文本描述即可获得高质量的视觉作品。同时，用户要求在生成图像后能够直接进行无缝编辑，如去除图像中的冗余物体、扩展构图比例、一键抠图或修复模糊画质。此外，由于涉及个人创意，用户对数据的隐私性和系统的运行稳定性有较高要求，倾向于本地化处理以避免数据泄露。

### 3.2.2 业务需求分析

<填写业务需求分析>

### 3.2.3 功能需求分析

文生图功能： 支持正/反向提示词输入，调节采样步数、尺寸、种子值等参数生成图像。

AI 智能编辑功能： 提供 AI 擦除（修复蒙版区域）、AI 扩图（向四周延伸画面）、AI 去背景、AI 提升解析度（4 倍无损放大）。

交互式画布： 提供支持缩放、平移及蒙版绘制的图形化界面。

作品管理： 能够保存历史生成记录，支持图片的预览、下载和二次编辑。

<填写功能需求分析，从系统中的八大功能以及“进度保存”去出发，每个功能一行内容>

### 3.2.4 业务建模

<从用户的角度去分析，并画出文生图与图像智能编辑系统用例图>

## 3.3 用例规约

<在这里列出所有用例>

### 3.3.1 创作生成模块用例

<填写文生图用例规约表>

### 3.3.2 编辑类功能模块用例

1. <填写AI擦除用例规约表>
2. <填写AI扩图用例规约表>
3. <填写AI重绘用例规约表>
4. <填写智能选区用例规约表>

### 3.3.3 非编辑类功能模块用例

1. <填写去背景用例规约表>
2. <填写超分辨率用例规约表>
3. <填写修复人脸用例规约表>

## 3.4 系统可行性分析

### 3.4.1 技术可行性

本系统在技术实现上具有高度可行性。

首先，算法层面，以 Stable Diffusion 为核心的扩散模型及 Real-ESRGAN 等超分辨率模型均已开源且具备成熟的 Python API (Hugging Face diffusers)，为系统核心功能的实现提供了可靠的技术支撑。

其次，架构层面，B/S 架构配合 FastAPI 异步框架能有效解决 AI 推理过程中的高延迟问题，通过任务队列和状态轮询机制可保证 Web 端的响应流畅性。

最后，硬件层面，现代消费级显卡（如 NVIDIA RTX 系列）已具备运行此类模型的能力，配合 CUDA 加速技术，使得本地化部署具备了实际运行的算力基础。

### 3.4.2 经济可行性

本系统的经济可行性主要体现在开发与运行成本的控制上。

系统核心组件全部基于开源协议（如 Apache 2.0、CreativeML Open RAIL-M 等），无需支付昂贵的商业授权费用。相比于订阅 Midjourney、DALL-E、Nano Banana 等商用云服务，本系统采用本地化部署模式，极大地降低了长期的带宽成本和单次生成的算力开销。对于个人创作者或小微企业而言，仅需一次性的硬件投入即可获得持续的 AI 生产力，具有显著的经济优势。

### 3.4.3 操作可行性

系统在操作流程设计上力求简洁直观。通过现代化的 Web 前端（React），将复杂的 AI 参数（如 CFG Scale、Seed 等）封装为易于理解的可视化滑块或预设选项。核心交互环节——智能画布编辑器，模仿了主流设计软件的操作习惯，支持快捷键和鼠标滚轮操作。用户无需具备深度学习背景，仅通过简单的“输入文字”和“鼠标涂抹”即可完成高质量的图像处理任务，操作门槛低，易用性强。

### 3.4.4 系统安全性分析

<系统安全性分析>

## 3.5 系统开发环境

本系统的开发涉及深度学习推理环境与 Web 全栈开发环境的整合。具体开发环境配置如表 3.4 所示。

表格 3.4

| **环境类别**       | **工具/名称与版本**                                 |
| ------------------ | --------------------------------------------------- |
| **操作系统**       | Ubuntu 24.04 LTS                                    |
| **硬件环境**       | NVIDIA GeForce RTX 4080 (16GB 显存)                 |
| **前端开发工具**   | Visual Studio Code 1.112.0                          |
| **后端开发环境**   | Visual Studio Code 1.112.0 / Python 3.10.20         |
| **数据库系统**     |                                                     |
| **AI 核心框架**    | PyTorch 2.x + CUDA 11.8 / 12.1                      |
| **Web 框架**       | FastAPI 0.10x (Backend) / React 18 (Frontend)       |
| **AI 算法库**      | Hugging Face Diffusers, Transformers, rembg, OpenCV |
| **版本控制与部署** | Git, Docker                                         |
| **API 调试工具**   | Postman / FastAPI Swagger UI                        |

# **第4章 系统设计**

## 4.1 系统架构设计

本系统采用了典型的 B/S（Browser/Server）架构，充分利用了 Web 技术的跨平台特性与后端 AI 算力的集中调度能力。系统架构分为展示层、业务逻辑层和算力支撑层。<补充内容>



系统架构设计如图 4.1 所示。

<图 4.1系统架构设计图>

## 4.2 系统功能模块整体设计

<补充内容>

## 4.3 系统功能模块详细设计

### 4.3.1 文生图创作模块设计

文生图是系统的核心生成功能。用户通过输入文本描述，系统调用底层扩散模型生成图像。<补充内容>

文生图流程： <简要描述文生图功能具体使用流程>

文生图序列图如图 4.2 所示。

<图 4.2 文生图序列图>

<文生图模块设计的相关类，其相关类设计如图>

<图 4.3 文生图模块设计相关类图>

### 4.3.2 AI擦除模块设计

AI 擦除允许用户通过蒙版绘制，让 AI 智能修复或移除图像中的特定区域。<补充内容>

AI 擦除流程： <简要描述AI擦除功能具体使用流程>

AI 擦除序列图如图 4.4 所示。

<AI擦除模块设计的相关类，其相关类设计如图>

<图 4.5 AI擦除模块设计相关类图>

### 4.3.3 AI扩图模块设计

<格式参考如上>

### 4.3.4 AI重绘模块设计

<格式参考如上>

### 4.3.5 去背景模块设计

<格式参考如上>

### 4.3.6 超分辨率模块设计

<格式参考如上>

### 4.3.7 修复人脸模块设计

<格式参考如上>

### 4.3.8 智能选区模块设计

<格式参考如上>

### 4.3.9 用户工作区模块设计

<格式参考如上>

## 4.4 接口设计

本系统遵循 RESTful API 规范进行接口设计。<由于 AI 业务逻辑的特殊性，图像数据传输主要采用 multipart/form-data（上传图片及蒙版）与 application/json（传输提示词及生成参数）相结合的方式。以下选取系统中具有代表性的核心接口进行描述。参考，可修改>

### 4.4.1 创作型模块接口设计

1\. 文生图接口

接口说明： 用户选择完模型后，提交提示词及参数，触发后台 AI 推理任务。

请求方法： <POST/GET/>

请求路径： </api/v1/generation/text-to-image>

请求参数： 请求体数据结构如表 4.1 所示。

表格 4.1文生图接口请求体数据结构表

| 属性            | 类型    | 说明                               |
| --------------- | ------- | ---------------------------------- |
| prompt          | String  | 正向提示词，描述画面内容，非空     |
| negative_prompt | String  | 反向提示词，排除不需要的内容，可选 |
| steps           | Integer | 采样步数，默认 20，范围 1-50       |
| width           | Integer | 图像宽度，需为 8 的倍数            |
| height          | Integer | 图像高度，需为 8 的倍数            |
| seed            | Long    | 随机种子，用于复现生成结果，可选   |

响应参数： 响应成功状态下，data 字段数据结构如表 4.2 所示。

表格 4.2文生图接口响应参数数据结构表

| 属性      | 类型   | 说明                        |
|-----------|--------|-----------------------------|
| image_id  | String | 图像在系统中的唯一标识符    |
| image_url | String | 生成图像的访问路径/下载地址 |
| status    | String | 任务状态（Success/Failed）  |

### 4.4.2 编辑类模块接口设计

1. AI 擦除接口

接口说明： 上传原图与绘制好的蒙版，进行局部区域修复。

请求方法： POST

请求路径： /api/v1/edit/inpaint

请求参数： 采用 form-data 格式，数据结构如表 4.3 所示。

表格 4.3 AI 擦除接口请求参数表

| 属性   | 类型   | 说明                             |
|--------|--------|----------------------------------|
| image  | File   | 待处理的原始图像文件，非空       |
| mask   | File   | 蒙版图像文件（黑白二值图），非空 |
| prompt | String | 针对蒙版区域的重绘提示词，可选   |

2. AI扩图接口
3. AI重绘接口
4. 智能选区接口

### 4.4.3 非编辑类模块接口设计

1. 去背景接口
2. 超分辨率接口
3. 修复人脸接口

### 4.4.4 工作区模块接口设计



## 4.5 数据库设计

### 4.5.1 CDM图

<这里写清楚文生图与图像智能编辑系统都有哪些实体，每个实体之间用“、”号隔开>

<这里写清楚每个实体都有哪些属性，每个实体占用一行，属性之间用“、”号隔开>

<这里写清楚各个实体之间的关系：谁与谁是一对多；谁与谁是一对一；谁与谁是多对多；>

<文生图与图像智能编辑系统CDM图>

### 4.5.2 数据库表结构设计

本系统共设计有 8 张核心表：用户表（tb_user）、图像历史表（tb_image_history）、模型参数表（tb_model_config）、生成任务记录表（tb_generation_task）、插件处理日志表（tb_plugin_record）、模型注册表（tb_model_registry）、用户偏好设置表（tb_user_preference）、系统操作审计表（tb_operation_audit）。各表详细结构如下：

1\. 用户表（tb_user）： 存储系统用户信息。

| 字段名      | 类型        | 必填 | 说明                  |
|-------------|-------------|------|-----------------------|
| user_id     | INT         | 是   | 用户 ID（主键，自增） |
| username    | VARCHAR(32) | 是   | 账户名（唯一）        |
| password    | VARCHAR(64) | 是   | 加密后的密码          |
| create_time | DATETIME    | 是   | 注册时间              |

2\. 图像历史表（tb_image_history）： 存储用户生成的图像元数据。

| 字段名      | 类型         | 必填 | 说明                                         |
|-------------|--------------|------|----------------------------------------------|
| img_id      | UUID         | 是   | 图像唯一 ID（主键）                          |
| user_id     | INT          | 是   | 所属用户 ID（外键）                          |
| file_path   | VARCHAR(255) | 是   | 图像在服务器的存储路径                       |
| task_type   | TINYINT      | 是   | 任务类型（0:文生图, 1:擦除, 2:扩图, 3:去背） |
| create_time | DATETIME     | 是   | 生成时间                                     |

3\. 模型参数表（tb_model_config）： 记录单次生成图像时所使用的具体 AI 参数，以便用户后续复现（Seed）或查看。

| 字段名     | 类型        | 必填 | 说明                            |
|------------|-------------|------|---------------------------------|
| config_id  | INT         | 是   | 参数 ID（主键）                 |
| img_id     | UUID        | 是   | 关联的图像 ID（外键）           |
| prompt     | TEXT        | 否   | 正向提示词内容                  |
| steps      | INT         | 否   | 采样迭代步数                    |
| seed       | BIGINT      | 否   | 生成时使用的随机种子            |
| model_name | VARCHAR(50) | 是   | 使用的基础模型名称（如 SD-1.5） |

4\. 生成任务记录表（tb_generation_task）：由于 AI 生成为耗时操作，本表用于追踪异步任务的状态与处理效率。

| 字段名     | 类型        | 必填 | 说明                                           |
|------------|-------------|------|------------------------------------------------|
| task_id    | UUID        | 是   | 任务唯一标识（主键）                           |
| user_id    | INT         | 是   | 任务发起者 ID                                  |
| task_type  | VARCHAR(20) | 是   | 任务类型（txt2img, inpaint, outpaint）         |
| status     | TINYINT     | 是   | 任务状态（0:待处理, 1:推理中, 2:成功, 3:失败） |
| error_msg  | TEXT        | 否   | 任务失败时的异常报错信息                       |
| duration   | FLOAT       | 否   | 模型推理总耗时（单位：秒）                     |
| start_time | DATETIME    | 是   | 任务进入队列的时间                             |

5\. 插件处理日志表（tb_plugin_record）：记录针对图像调用的辅助 AI 功能（如 SAM 分割、超分辨率），体现系统的扩展性。

| 字段名        | 类型        | 必填 | 说明                                       |
|---------------|-------------|------|--------------------------------------------|
| record_id     | INT         | 是   | 记录 ID（主键）                            |
| source_img_id | UUID        | 是   | 处理前的原图 ID                            |
| output_img_id | UUID        | 是   | 处理后的结果图 ID                          |
| plugin_name   | VARCHAR(50) | 是   | 调用的插件名称（如 RealESRGAN, SAM）       |
| input_params  | JSON        | 否   | 插件特有的输入参数（如缩放倍数、点击坐标） |
| create_time   | DATETIME    | 是   | 插件执行时间                               |

6\. 模型注册表（tb_model_registry）：管理后端可调用的各种大模型权重信息，支持多模型动态切换逻辑。

| 字段名       | 类型         | 必填 | 说明                               |
|--------------|--------------|------|------------------------------------|
| model_id     | VARCHAR(50)  | 是   | 模型逻辑 ID（主键，如 sd_xl_1.0）  |
| display_name | VARCHAR(100) | 是   | 前端显示的直观名称                 |
| model_type   | VARCHAR(20)  | 是   | 模型类别（Base, LoRA, ControlNet） |
| local_path   | VARCHAR(255) | 是   | 权重文件在服务器的物理存放位置     |
| is_active    | BOOLEAN      | 是   | 当前模型是否处于启用状态           |
| vram_usage   | INT          | 否   | 模型推理预估显存占用（单位：MB）   |

7\. 用户偏好设置表（tb_user_preference）：存储用户个性化的界面配置与默认参数，提升人机交互体验。

| 字段名        | 类型        | 必填 | 说明                              |
|---------------|-------------|------|-----------------------------------|
| pref_id       | INT         | 是   | 设置项 ID（主键）                 |
| user_id       | INT         | 是   | 关联用户 ID（一比一关系）         |
| default_model | VARCHAR(50) | 否   | 用户首选的默认生成模型            |
| theme_mode    | VARCHAR(10) | 是   | 界面主题（Light / Dark / System） |
| language      | VARCHAR(10) | 是   | 界面语言（zh-CN / en-US）         |
| auto_save     | BOOLEAN     | 是   | 是否开启生成结果自动存入作品集    |

8\. 系统操作审计表（tb_operation_audit）：记录用户在系统内的关键操作行为，用于安全审计与系统优化。

| 字段名      | 类型         | 必填 | 说明                                         |
|-------------|--------------|------|----------------------------------------------|
| audit_id    | INT          | 是   | 审计 ID（主键，自增）                        |
| user_id     | INT          | 否   | 执行操作的用户（访客为 NULL）                |
| action      | VARCHAR(50)  | 是   | 操作类型（如：用户登录、导出图像、清空缓存） |
| ip_address  | VARCHAR(45)  | 是   | 操作时的客户端 IP 地址                       |
| user_agent  | VARCHAR(255) | 否   | 客户端浏览器环境信息                         |
| create_time | DATETIME     | 是   | 操作发生的精确时间                           |

# **第5章 系统实现**

在智能图像编辑系统的后端工程采用了前后端分离的架构，各组件模块分工明确，共同支撑起 AI 模型推理与数据调度的核心逻辑。

本系统后端采用 Visual Studio Code - Insiders 进行开发，项目工程结构如图 5.1 所示。

![](media/7ec042076f12c0969312a209cff98315.png)

图 5.1 后端代码结构图

该结构中各部分模块的介绍如下：

1.artie 主包：存放系统的核心逻辑实现。其中 api.py 作为路由中心，基于 FastAPI 框架定义了所有的 RESTful 接口，负责解析前端请求并分发至相应的处理函数。

2.model 包：系统最核心的算力封装模块。该包集成了 LaMa、Stable Diffusion、AnyText 等多种深度学习模型的推理逻辑，通过封装 BaseModel 基类，实现了不同 AI 算法在输入预处理、张量运算及输出解码上的统一调用。

3.plugins 包：存放功能增强插件。包括基于 RealESRGAN 的超分辨率重建、基于 Segment Anything (SAM) 的智能分割以及人脸修复等算子。插件化的设计使得系统具备良好的横向扩展性，能够根据用户需求动态集成新的图像处理技术。

4.db 包：数据持久化层模块。利用 SQLAlchemy ORM 技术实现对数据库的操作。其中 models.py 定义了应用配置、任务记录等实体模型，crud.py 则封装了对数据库的创建、查询、更新和删除操作，确保了业务逻辑与底层存储的解耦。

5.file_manager 包：文件管理与存储后端。负责处理图像上传、临时文件缓存及成品图的分发。该包支持不同的存储驱动（如本地磁盘或云端存储），并通过 utils.py 提供了图像格式校验和 Base64 编码转换等辅助功能。

6.model_manager.py：单例管理类，负责显存资源的调度与模型生命周期管理。由于 AI 模型占用显存较高，该模块实现了模型的动态加载与回收机制，确保在有限的硬件资源下实现多任务的平稳切换。

7.runtime.py：执行环境配置模块。负责初始化计算设备（如 CUDA 或 CPU），并维护系统运行时的全局上下文信息。

8.schema.py：数据验证模块。基于 Pydantic 库定义了前后端交互的数据协议，对所有 API 的输入参数和返回结果进行严格的类型检查与格式校验。

本系统前端采用 Visual Studio Code - Insiders进行开发，项目工程结构如图 5.2 所示。

![](media/24142efb5b603685851379189814f9eb.png)

图 5.2 前端代码结构图

该结构中各部分模块的介绍如下：

1.components 文件夹：存放 React 组件化视图。包含 Editor.tsx 画布组件、SidePanel 侧边栏工具栏以及各类功能 Tab（如局部重绘、扩图等），通过组件的解耦与复用实现了高度交互的 Web 界面。

2.hooks 文件夹：存放自定义 React Hooks。例如 useInpaint.ts 封装了与后端重绘接口的异步交互逻辑，useImage.ts 负责管理画布中的图像状态，使得业务逻辑从 UI 组件中剥离，提升了代码的可维护性。

3,lib 文件夹：存放前端的核心库文件。包括 API 客户端定义、全局状态管理的 Store 配置以及系统常量定义，是前端应用的数据流通中枢。

4.assets 文件夹：存放静态资源文件。包含系统中使用的 UI 图标、默认素材图片以及用于界面展示的静态资源。

5,types 文件夹：存放 TypeScript 类型定义文件。通过严谨的接口（Interface）定义，规范了前端内部及与后端 API 交互的数据结构，利用静态检查在开发阶段捕获潜在的逻辑错误。

6,tyles 文件夹：存放全局样式配置。基于 Tailwind CSS 框架实现了响应式布局与深色模式支持，确保了系统在不同设备上的视觉一致性。

7.App.tsx：应用程序的主入口组件，负责整体界面的路由嵌套与主题配置。

## 5.1 开发环境与配置实现

本系统的开发与运行依赖于深度学习环境与 Web 服务环境的有机结合。服务端采用 Python 3.10 作为核心开发语言，利用 PyTorch 2.0+ 框架进行张量运算与模型推理。为了保障扩散模型的高效运行，系统底层通过 CUDA 11.8 指令集与 NVIDIA GPU 进行硬件加速。前端部分则基于 Node.js 环境，利用 Vite 构建工具和 TypeScript 语言完成开发。系统配置信息统一管理在 config.json 中，包括模型存储路径、运行设备（CPU/GPU）选择以及 API 端口设置，确保了系统在不同硬件环境下的可迁移性。

## 5.2 服务端核心功能实现

### 5.2.1 模型调度管理模块实现

由于本系统集成了 LaMa、Stable Diffusion、SAM 等多种大型深度学习模型，显存资源的合理分配至关重要。系统在 artie/model_manager.py 中实现了一套动态加载与单例管理机制。该模块负责根据前端请求的功能类型，实时将对应的模型权重加载至显存，并在切换功能时执行显存回收操作。

后端模型加载的核心代码如下：

| \# artie/model_manager.py<br>def get_model(self, model_name: str):<br> if model_name not in self.models:<br> log.info(f"正在加载模型：{model_name}")<br> \# 根据模型名称初始化对应的实现类<br> model_class = MODEL_MAP.get(model_name)<br> if not model_class:<br> raise ValueError(f"未定义的模型类型: {model_name}")<br> \# 实例化模型并移动至指定设备（GPU/CPU）<br> self.models[model_name] = model_class(self.device)<br> return self.models[model_name] |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

### 5.2.2 图像处理 API 路由实现

系统基于 FastAPI 框架在 artie/api.py 中定义了标准化的 RESTful 接口。以核心的局部重绘（Inpainting）功能为例，后端接收前端传递的 Base64 编码图像和蒙版数据，利用 Pydantic 进行参数校验后，调用扩散模型管线进行推理。

后端局部重绘接口实现的核心代码如下：

| \# artie/api.py<br>@app.post("/api/inpaint")<br>async def inpaint(request: InpaintRequest):<br> \# 解析前端发送的图像与蒙版数据<br> image = decode_base64_to_image(request.image)<br> mask = decode_base64_to_image(request.mask)<br> <br> \# 获取已加载的重绘模型实例<br> model = model_manager.get_model(request.model_id)<br> <br> \# 执行推理，获取处理后的 PIL 图像对象<br> result_img = await model.run(image, mask, request.params)<br> <br> \# 将结果转换为 Base64 返回给前端<br> return {"status": "success", "image": encode_image_to_base64(result_img)} |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

### 5.2.3 实时进度推送实现

针对生成式任务耗时较长的特性，系统集成了 Socket.IO 技术以提供实时的推理进度反馈。在推理循环中，后端会捕获扩散模型的采样步数，并通过 WebSocket 频道将百分比进度推送至前端。

实时推送的核心逻辑代码如下：

| \# artie/model/sd.py (回调部分)<br>def progress_callback(self, step: int, timestep: int, latents: torch.FloatTensor):<br> \# 计算当前进度百分比<br> percent = int((step / self.num_inference_steps) \* 100)<br> \# 通过 Socket 异步推送至前端<br> sio.emit("diffusion_progress", {"progress": percent, "step": step}) |
|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

## 5.3 前端交互模块实现

### 5.3.1 交互式编辑器与画布实现

前端核心组件 web_app/src/components/Editor.tsx 基于 HTML5 Canvas 研发。该组件实现了多层渲染逻辑：底层展示原始图像，上层则为用户提供透明的蒙版绘制层。通过监听鼠标或触控事件，系统能够记录用户的“涂抹”轨迹，并将其转化为二值化掩码图。

蒙版绘制逻辑的关键代码如下：

| // web_app/src/components/Editor.tsx<br>const handleMouseMove = (e: React.MouseEvent) =\> {<br> if (!isDrawing) return;<br> const ctx = canvasRef.current.getContext('2d');<br> // 设置笔刷参数<br> ctx.lineWidth = brushSize;<br> ctx.lineCap = 'round';<br> ctx.strokeStyle = 'rgba(255, 0, 0, 0.5)'; // 半透明红色提示<br> // 连接当前坐标与上一时刻坐标<br> ctx.lineTo(e.nativeEvent.offsetX, e.nativeEvent.offsetY);<br> ctx.stroke();<br>}; |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

### 5.3.2 异步任务状态管理

为了应对复杂的图像处理工作流，系统在 web_app/src/lib/states.ts 中通过 Zustand 或 Recoil 维护全局状态。当用户触发“生成”按钮时，系统会进入 processing 状态，锁定操作界面并显示实时进度条，直至后端返回结果图像。

前端状态转换核心代码如下：

| // web_app/src/hooks/useInpaint.tsx<br>export const useInpaint = () =\> {<br> const setStatus = useSetRecoilState(statusState);<br> const executeInpaint = async (data: InpaintData) =\> {<br> setStatus('processing'); // 切换至处理中状态<br> try {<br> const response = await api.post('/inpaint', data);<br> setResultImage(response.data.image);<br> setStatus('idle'); // 任务结束，恢复空闲状态<br> } catch (error) {<br> toast.error("图像处理失败");<br> setStatus('error');<br> }<br> };<br>}; |
|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

## 5.4 辅助增强模块实现

### 5.4.1 智能分割与去背景实现

系统集成了 Segment Anything (SAM) 插件，用于实现自动化目标提取。用户只需在图像上点击感兴趣的点，前端 InteractiveSegTab.tsx 组件会将坐标发送至后端。后端利用 artie/plugins/interactive_seg.py 中的算法生成精准的物体轮廓，配合 OpenCV 算子实现一键去背景功能。

### 5.4.2 超分辨率与人脸修复实现

在生成任务完成后，用户可选择调用 RealESRGAN 插件提升图像清晰度。在 artie/plugins/realesrgan.py 中，源码通过残差密集网络（RRDBNet）对低分辨率张量进行 4 倍放大。同时，针对人脸生成可能出现的畸变，系统调用 GFPGAN 插件进行特征对齐与细节补偿，确保了视觉产出的专业性。

# **第6章 系统测试**

## 6.1 测试方案及测试用例

### 6.1.1 发送数据

（以下略）

### 6.1.2 接收数据

（以下略）

## 6.2测试方法

# **第7章 结论**

（以下略）

# **参考文献**

参考文献总体样本：

[1]王中南,王春乐,陈永强. 利用扩散模型解决SAR图像生成高频分量缺失问题[J].科学技术创新,2025,(20):122-126.

[2]何艳芬,侯珏,邱雪琳,等. 基于风格迁移的云锦图像生成系统的构建[J].毛纺科技,2025,53(08):45-53.DOI:10.19333/j.mfkj.20240503801.

[3]王星艺,刘新军,童建民,等. 一种双投影大型车辆检测系统辐射图像库自动生成方法[J].同位素,2025,38(05):470-476.

[4]李亚琴,吴振昊,朱卓晖. GiantAI：面向建筑概念设计的生成式图像智能辅助系统开发与实践[J].华中建筑,2025,43(06):54-59.DOI:10.13942/j.cnki.hzjz.2025.06.036.

[5]李夏童.基于深度学习的半色调图像恢复方法研究[D].北京印刷学院,2025.DOI:10.26968/d.cnki.gbjyc.2025.000108.

[6]孙达.基于手绘草图的唐卡花叶元素图像生成研究及应用[D].青海师范大学,2025.DOI:10.27778/d.cnki.gqhzy.2025.000503.

[7]孙海洋.局部遮挡人脸图像的修复识别系统设计与实现[D].黑龙江大学,2025.DOI:10.27123/d.cnki.ghlju.2025.002097.

[8]张倩. 基于AICG技术的计算机视觉系统图像生成方法研究[J].信息记录材料,2025,26(05):145-147.DOI:10.16009/j.cnki.cn13-1295/tq.2025.05.055.

[9]隆良梁,狄飞. 基于人工智能算法的图像识别与生成研究[J].电子元器件与信息技术,2024,8(09):86-89.DOI:10.19772/j.cnki.2096-4455.2024.9.026.

[10]何金婷.基于扩散模型的沙发概念设计方案智能生成系统构建研究[D].中南林业科技大学,2024.DOI:10.27662/d.cnki.gznlc.2024.000388.

[11]陈思旭.灰度图像色彩生成与评估系统设计与实现[D].北京邮电大学,2024.DOI:10.26969/d.cnki.gbydu.2024.002035.

[12]王丽璇.基于扩散模型文本生成图像的研究与实现[D].兰州交通大学,2024.DOI:10.27205/d.cnki.gltec.2024.000293.

[13]赵善松.基于对抗网络的文本生成图像系统的设计与实现[D].黑龙江大学,2024.DOI:10.27123/d.cnki.ghlju.2024.000375.

[14]钟益华. 一个基于文本智能生成图像技术的英语在线阅读系统的设计与实现[J].电脑知识与技术,2022,18(08):78-80.DOI:10.14004/j.cnki.ckt.2022.0481.

[15] Rombach R, Blattmann A, Lorenz D, et al. High-resolution image synthesis with latent diffusion models[C]//Proceedings of the IEEE/CVF conference on computer vision and pattern recognition. 2022: 10684-10695.

[16] Suvorov R, Logacheva E, Mashikhin A, et al. Resolution-robust large mask inpainting with fourier convolutions[C]//Proceedings of the IEEE/CVF winter conference on applications of computer vision. 2022: 2149-2159.

[17] Wang X, Xie L, Dong C, et al. Real-esrgan: Training real-world blind super-resolution with pure synthetic data[C]//Proceedings of the IEEE/CVF international conference on computer vision. 2021: 1905-1914.

[18] Qin X, Zhang Z, Huang C, et al. U2-Net: Going deeper with nested U-structure for salient object detection[J]. Pattern recognition, 2020, 106: 107404.

[19] Hertz A, Mokady R, Tenenbaum J, et al. Prompt-to-prompt image editing with cross attention control[J]. arXiv preprint arXiv:2208.01626, 2022.
